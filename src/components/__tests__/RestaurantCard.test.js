import { render, screen } from "@testing-library/react";
import RestaurantCard from "../RestaurantCard";
import "@testing-library/jest-dom";
import MOCK_DATA from "../mocks/resCarMock.json";

test("should render Restaurent Component with props Data", () => {

    // it shoes error that you have to destructring 


//   render(<RestaurantCard resData={MOCK_DATA} />);

//   const nameCart = screen.getByText("Salads");

//   expect(nameCart).toBeInTheDocument();
});
